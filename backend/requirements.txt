# Personal RAG Chatbot — backend runtime deps (Phase 1)
# Install:  pip install -r requirements.txt

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# Config / validation
pydantic>=2.9
pydantic-settings>=2.6
email-validator>=2.2

# HTTP client (provider calls)
httpx>=0.27

# Vector DB
qdrant-client>=1.12
pinecone>=5.0

# Auth + crypto
pyjwt>=2.9
argon2-cffi>=23.1
cryptography>=43.0

# Users DB (optional — falls back to in-memory if unset)
supabase>=2.9

# Ingestion
pypdf>=5.1
tiktoken>=0.8

# ── Tests (dev) ──
pytest>=8.3
pytest-asyncio>=0.24
